#include <stdio.h>



int main() {

int score = 90.5;

printf("Score: %d\n", score);

return 0;

}



// สรุปกรณีที่1: ไม่สามารถคอมไพล์ได้เนื่องจากการใช้ตัวแปร score เป็น float แต่ใช้คำสั่ง int ชึงเอาไว้ใช้กับตัวแปรที่เป็นจำนวนเต็ม

